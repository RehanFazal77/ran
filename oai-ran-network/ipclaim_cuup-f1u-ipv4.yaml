apiVersion: ipam.resource.nephio.org/v1alpha1
kind: IPClaim
metadata:
  name: cuup-f1u-ipv4
  annotations:
    config.kubernetes.io/local-config: "true"
    specializer.nephio.org/for: workload.nephio.org/v1alpha1.NFDeployment.cuup
    specializer.nephio.org/owner: req.nephio.org/v1alpha1.Interface.f1u
spec:
  kind: network
  selector:
    matchLabels:
      nephio.org/address-family: ipv4
      nephio.org/cluster-name: placeholder
      nephio.org/network-name: f1
  networkInstance:
    name: vpc-cudu-f1
status: {prefix: 172.5.1.1/16, gateway: ""}
